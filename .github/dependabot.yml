# Generated from validityBase/vbase-github-internal templates.
# Do not edit rendered files manually; update the source template in validityBase/vbase-github-internal.
version: 2
updates:
  - package-ecosystem: "pip"
    # Dependabot scans the root pip-compile graph, including requirements files
    # under /docs, from this single update entry.
    directory: "/"
    schedule:
      interval: "daily"
      time: "00:00"
      timezone: "Etc/UTC"
    versioning-strategy: "increase-if-necessary"
    groups:
      pip-version-updates:
        applies-to: "version-updates"
        patterns:
          - "*"
      pip-security-updates:
        applies-to: "security-updates"
        patterns:
          - "*"
    ignore:
      - dependency-name: "pip"
        update-types:
          - "version-update:semver-major"
          - "version-update:semver-minor"
